## Packages
date-fns | Date formatting and manipulation
recharts | Charts for usage history if needed (though not explicitly requested, good for stats)
framer-motion | Smooth animations for progress bars and transitions

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
API is at /api/state for full state
